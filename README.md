##Commands to clone the repo and run the project:

```
yarn install
react-native run-android
```

##Commands used when creating this module:

```
yarn add react-navigation
yarn add react-native-gesture-handler
yarn add axios
react-native link
```